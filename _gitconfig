[user]
	name = pright
	email = pright.xj@gmail.com
[http]
	sslVerify = false
[push]
	default = simple
	# default = matching
[branch]
	autosetuprebase = always
[diff]
	tool = meld
[difftool]
	prompt = false
[merge]
	tool = meld
[mergetool]
	prompt = false
[sendemail]
	smtpserver = smtp.163.com
	smtpserverport = 25
	smtpuser = pright-xj@163.com
[github]
	user = pright
#[gc]
	#auto = 0
[alias]
    cl = "clone"
    rm = "remote"
    sm = "submodule"
    sma = "submodule add"
    smu = "submodule update --init" 
    smr = "submodule update --init --recursive" 
    smrr = "submodule update --init --recursive --remote" 
    df = "diff"
    cp = "cherry-pick"
    st = "status"
    cm = "commit"
    ca = "commit --amend"
    pl = "pull"
    ps = "push"
    co = "checkout"
    br = "branch"
    rb = "rebase"
    pa = "!f(){ git pull \"$@\" && git submodule update --init --recursive; }; f"
    par = "!f(){ git pull \"$@\" && git submodule update --init --recursive --remote; }; f"
[core]
	whitespace = trailing-space,space-before-tab
	quotepath = false
[apply]
	whitespace = fix
